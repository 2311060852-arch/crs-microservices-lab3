**/.idea/
**/.gradle/
**/build/
**/out/
**/target/
*.iml

# Local-only credentials. These files are included in the returned ZIP so the
# project runs on this machine, but Git will ignore them on future commits.
**/src/main/resources/application-local.properties
.env
